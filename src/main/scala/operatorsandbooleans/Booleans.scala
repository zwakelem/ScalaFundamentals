package operatorsandbooleans


/**
 * Logical operators work like Java
 *
 * && => and
 * || => or
 * ! => not
 * > => greater than
 * < => less than
 */
@main
def booleans():Unit = {
  val a = true
  println(a)
  println(a.getClass)
}
